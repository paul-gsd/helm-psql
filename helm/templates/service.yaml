apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - protocol: TCP
      port: 5432
      targetPort: {{ .Values.psql.port }}
  type: {{ .Values.service.type }}
